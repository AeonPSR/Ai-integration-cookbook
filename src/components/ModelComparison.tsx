/**
 * Runs the same prompt against multiple models and displays results side-by-side.
 */

export default function ModelComparison() {
  return (
    <div className="grid gap-4 md:grid-cols-3">
      {/* TODO: one column per model — prompt input shared, output per model */}
    </div>
  );
}
