/**
 * Interactive sandbox: user pastes input, sees LLM output in real-time.
 * Requires API key to function.
 */

export default function LiveSandbox() {
  return (
    <div className="rounded-lg border p-4">
      <h3 className="mb-2 font-semibold">Try It</h3>
      {/* TODO: input textarea, model selector, run button, streaming output */}
    </div>
  );
}
